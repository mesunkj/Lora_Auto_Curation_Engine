這是一份為您全面升級後的完整需求計畫清單。這份架構不僅徹底分離了「安檢」與「量產」兩個專案，更導入了輕量級的起飛前 CPU 盤點機制，將使用者的時間與 GPU 算力浪費降至最低。

# **📄 底圖防呆安檢與精準量產 需求計畫清單 (Decoupled Architecture v2.0)**

## **📌 全域共用設定 (配置於 config.py)**

為了讓兩個獨立專案能夠互相溝通，必須在共同的設定檔中定義以下絕對路徑：

* GLOBAL\_POSE\_CACHE\_FILE \= "./output/registry/global\_pose\_audit.json"：全域歷史底圖安檢紀錄檔。  
* TASK\_LIST\_DIR \= "./output/task\_lists"：純文字生產工單的統一存放目錄。

## **📡 階段一：智慧底圖雷達與工單生成模組 (所屬專案：資料篩選/前處理)**

**核心目標**：透過輕量文字比對與 InsightFace 物理掃描，過濾不合格底圖，並產出純文字的「生產工單」。

### **0\. 起飛前安檢與人機互動 (純 CPU 輕量模式)**

* **載入字典**：程式啟動時，只用 CPU 讀取 GLOBAL\_POSE\_CACHE\_FILE (包含 accept 與 reject 兩個陣列)。  
* **輕量盤點**：純文字比對使用者指定的「目標底圖目錄」（如包含 252 張圖的資料夾），將目錄內的圖片分為三類：  
  * 已存在於 accept 清單。  
  * 已存在於 reject 清單。  
  * 全新且未知的圖片（待檢定）。  
* **戰情報告與互動閥門**：於終端機印出盤點結果：✅ 歷史已接受: 35 張 | ❌ 歷史已拒絕: 102 張 | ❓ 未知待檢定: 115 張  
  👉 是否準備啟動 GPU 載入模型，檢定這 115 張未知圖片？(y/N):  
* 使用者確認 (輸入 y) 後，才正式掛載高耗能的 InsightFace 模型進入下一步。

### **1\. 物理極限 (Physical Limits) 讀取與絕對建置**

* **讀取邊界**：去 lora\_registry.json 讀取該 Face ID 的 physical\_limits。  
* **自動建置機制**：若無此引數，程式自動去掃描「該 Face ID 驗收合格的 24 張黃金訓練圖」。  
* **白話文標準化**：抓取這 24 張圖特徵的「絕對最大值與最小值」，建立以下明確邊界：  
  * **臉頰寬度與下巴長度比例** (Aspect Ratio)：例如 0.8 \~ 1.2（防止底圖骨相不符/太方或太長）。  
  * **左右轉頭極限** (Yaw)：例如向左最多 \-25 度，向右最多 \+35 度。  
  * **上下仰俯極限** (Pitch)：例如低頭最多 \-10 度，抬頭最多 \+20 度。  
  * **左右歪頭極限** (Roll)：例如容許歪頭 ±15 度。

### **2\. 未知底圖安檢執行 (Pose Auditing)**

* **精準掃描**：InsightFace **只掃描**在第 0 步被歸類為「❓ 未知待檢定」的圖片（例如那 115 張），徹底免除重複運算。  
* **特徵判定**：  
  * 若臉部佔比低於全圖 5%（防 VAE 崩壞），判定為不合格。  
  * 將偵測到的臉型比例、左右轉頭、上下仰俯角度，與第 1 步定義的「絕對邊界」進行比對。  
* **分類收容**：全數落於邊界內者歸為 accept，任一超標者歸為 reject。

### **3\. 寫回快取與產出工單**

* **全域更新**：將本次新判定的結果路徑，追加寫回 GLOBAL\_POSE\_CACHE\_FILE。  
* **產出工單**：將本次任務的目標底圖中，所有判定為 accept（包含歷史已接受 \+ 本次新接受）的絕對路徑，匯出為純文字清單。  
* **檔案命名**：格式為 task\_\[Face\_ID\]\_\[YYYYMMDD\_HHMMSS\].txt，並儲存至 TASK\_LIST\_DIR

### **🧠 針對載入字典 (Global Cache) 的深度理解與運作邏輯**

**1\. 嚴格的層級資料結構 (Face\_ID 隔離)**  
全域快取字典必須嚴格遵照以 Face\_ID 為第一層 Key 的巢狀結構，確保不同模型的審查結果互不干擾。其結構如下：

JSON  
{  
    "Tzuyu": {  
        "accept": \["/路徑/圖A.jpg", "/路徑/圖B.jpg"\],  
        "reject": \["/路徑/圖C.jpg"\]  
    },  
    "Jennie": {  
        "accept": \["/路徑/圖C.jpg"\],   
        "reject": \["/路徑/圖A.jpg"\]  
    }  
}

*(備註：同一張 圖C.jpg 可以在 Tzuyu 被拒絕，但在 Jennie 被接受)*  
**2\. 啟動時的「精準定錨」與盤點邏輯**  
當階段一的雷達程式啟動，且使用者指定要處理 Tzuyu 時：

* **讀取與定錨**：程式開啟 global\_pose\_audit.json 後，只會去抓取 \["Tzuyu"\] 這個節點底下的資料。  
* **防呆初始化**：如果字典裡根本還沒有 Tzuyu 這個 Key，程式會直接將目標目錄下所有的底圖判定為「❓ 未知/待檢定」，並在記憶體中為 Tzuyu 建立一組空的 accept 與 reject 陣列。  
* **純 CPU 文字比對**：如果找到了 Tzuyu 節點，程式只拿 Tzuyu 專屬的 accept 與 reject 陣列，去和目標目錄（如那 252 張圖）的絕對路徑進行 Python 的字串比對 (if img\_path in cache\["Tzuyu"\]\["accept"\])。

**3\. 寫回機制的獨立性**  
當使用 GPU 檢定完那些「未知的底圖」後，程式在寫回 json 檔案時，只會 append 到該 Face\_ID 專屬的陣列中，絕對不會污染或覆寫到其他明星的盤點紀錄。  
**4\. 起飛前的人機互動與 Face ID 選擇 (Pre-flight Face ID Selection)**

* **讀取註冊表並列舉選單**：在啟動純 CPU 輕量盤點之前，程式的第一個動作必須是讀取 lora\_registry.json，將內部所有已註冊的 Face ID（如 Tzuyu 等）撈出來，並在終端機印出選項列表。  
* **互動式指定目標**：暫停程式，由使用者輸入編號，明確選擇本次要進行底圖安檢的目標 Face\_ID。  
* **動態帶入盤點邏輯**：待使用者完成選擇後，系統才會把這個特定的 Face ID 當作 Key，帶入 GLOBAL\_POSE\_CACHE\_FILE 中去抓取專屬的 accept 與 reject 陣列，接著才真正開始執行該明星專屬的歷史紀錄對比與戰情報告。

## **🚀 階段二：批次量產精準打擊模組 (所屬專案：推論與產圖)**

**核心目標**：修改現有批次 UI，使其能吃進「階段一」開出的工單，直接進行 GPU 渲染。

### **1\. 介面選項擴充：工單路由攔截**

* 在原有的 \[路徑設定\] 階段，新增底圖來源選項：  
  * \[1\] 指定傳統底圖目錄掃描。  
  * \[2\] **載入雷達生產工單 (Task List)**。

### **2\. 自動列舉與掛載工單**

* 若使用者選擇 \[2\]，程式自動讀取 config.TASK\_LIST\_DIR **(⚠️ 引用來源：共用設定檔)**。  
* 過濾並列出該目錄下，檔名符合當前執行 Face ID 的所有 .txt 檔案 **(⚠️ 引用來源：階段一產出的工單檔案)**。  
* 於終端機印出選單（如：\[1\] task\_Tzuyu\_20260826\_0940.txt），供執行者輸入編號挑選。

### **3\. 絕對路徑直達車 (Bypass OS Listdir)**

* 解析使用者選定的 .txt 工單，將內部的一行行絕對路徑轉換為 Python 陣列 (List)。  
* **略過無效程序**：程式不再執行 os.listdir() 巡覽資料夾，也不再檢查 .jpg/.png 副檔名。  
* **全速渲染**：GPU 啟動，直接將 List 內的絕對路徑一張一張送進 管線 A (InsightFace \+ SD \+ LoRA) 進行最終渲染。確保送入 GPU 的每一張圖，都是完美落在模型知識舒適圈內的極品底圖。